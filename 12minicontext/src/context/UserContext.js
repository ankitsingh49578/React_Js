import React from "react";

const UserContext = React.createContext()           // this is also kind a method like useState
// context is nothing but kinda global variable

export default UserContext;